{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npx prisma db push && npx prisma generate && npx tsx src/server.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}